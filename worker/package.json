{
  "name": "gprocurement-finder",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend API for GProcurement Finder",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.0.0",
    "worker-mailer": "^1.2.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
